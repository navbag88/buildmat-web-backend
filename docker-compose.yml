version: '3.8'

services:
  buildmat-backend:
    image: ghcr.io/navbag88/buildmat-web-backend:${GITHUB_SHA}
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://dbaas-db-8513734-do-user-18444064-0.f.db.ondigitalocean.com:25060/buildmat?ssl-mode=REQUIRED
      - SPRING_DATASOURCE_USERNAME=doadmin
      - SPRING_DATASOURCE_PASSWORD=AVNS_XKupnlVA6SWGTfe-xLe
      - LOG_DIR=/mnt/volume-1776098194582/buildmat/logs
    depends_on:
      - db
    restart: always

  db:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=AVNS_XKupnlVA6SWGTfe-xLe
      - MYSQL_DATABASE=buildmat
    volumes:
      - db_data:/var/lib/mysql
    restart: always

volumes:
  db_data:
